// =============================================================================
// instance_registry_fwd.h
//
// Forward declarations for InstanceRegistry.
// Lets recovery_session_manager.h be included without pulling in the
// full instance_registry.h (which is Agent 2's deliverable).
// =============================================================================
#pragma once

namespace swordfare::research {
    class InstanceRegistry;
}
